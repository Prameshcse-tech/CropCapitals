# Local dev — leave as is if backend runs on localhost:5000
REACT_APP_API_URL=http://localhost:5000

# On Vercel, set this in Project Settings -> Environment Variables to your
# deployed Render backend URL instead, e.g.:
# REACT_APP_API_URL=https://cropcapitals-backend.onrender.com
